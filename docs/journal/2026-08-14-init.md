---
title: Initialized Werkbank
date: 2026-08-14
tags: [meta, setup]
summary: First-run setup completed — tool named Werkbank, a Jira-like ticket system for Claude Code agents; git repo, docs, shared skills and machine-wide block all in place.
outcome: done
---

# Initialized Werkbank

## What was asked

The user (German-speaking, non-technical interaction contract) asked for "something like
Jira for Claude Code": they or an agent write tickets, and other agents work them off
depending on who is assigned.

## What I did

- Interview: language = German (from the first message), purpose = ticket system for
  Claude Code agents, name = **Werkbank** (chosen from suggested options).
- `git init -b main`; machine-wide git identity already present (used as-is). Committed
  the pristine template first: `Initial commit: pristine template`.
- Personalized `CLAUDE.md` (name, description, language), wrote `docs/user/about.md`
  (German), indexed it in `docs/INDEX.md`, rewrote the `CHANGELOG.md` header in German
  with a setup entry. Commit: `Personalize template for Werkbank`.
- Installed all six shared skills (creating-skills, documenting, explaining-complexity,
  finding-knowledge, git-discipline, journaling), each version 1, into
  `~/.claude/skills/` — none were present before, so all were fresh copies.
- Appended the `developer-agent:start v1` block to `~/.claude/CLAUDE.md` (no block
  existed) with Werkbank's entry: `~/code/werkbank`.
- Gave the permissions explainer, wrote this entry, removed the UNINITIALIZED marker.

## What I tried that didn't work — and why

Nothing failed.

## Decisions made

- Kept the existing machine-wide git identity (Ed Rethardo; e-mail redacted before
  the public release — it remains visible in commit metadata) instead of
  setting a repo-local one — the skill allows any working identity and the user was not
  asked, per the init flow.

## Follow-ups

- The actual ticket system is not designed or built yet. Next session should start with
  the `explaining-complexity` skill and a design conversation: ticket storage format,
  how agents pick up assigned tickets, and what interface the user gets.
- Optional: user may rename the folder from `agent_ticket` to `werkbank`; the path in
  `~/.claude/CLAUDE.md` would then be stale (acceptable by design).
